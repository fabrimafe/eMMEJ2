# This folder contains an example of the pre-EMmej analysis data handaling
## see "Supplementary note 3. Data handling prior EMmej analysis"

```
├── add_ancesral_state_column.py -> A script that generate an ancestral state indication column based on allele frequency.
├── DGRP_annotation_intersections.sh -> A script that intersect the EMmej formated data with annotations.
├── DGRP_raw_data_to_EMmej_format.sh -> A script that converts a vcf format to EMmej input format.
└── README.md
```