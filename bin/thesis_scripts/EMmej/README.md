# This folder contains the scripts that envolve in EMmej pipline

```
├── EMmej.py -> A WEXAC specific script that operate the whole pipline.
├── EM_operator.py -> A script that perform the EM procedure.
├── Markov_model_operator.py -> A script that performes the probebalistic model.
├── README.md
└── RMdetector.py -> A script to perform the pattern detection steps.
```