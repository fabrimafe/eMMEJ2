# This folder contains the indel simulation related scripts 

```
├── DSBsimulator.py -> A script that generates the simulated indels
├── EMmej_set_cutoffs_using_ROC_curves.py -> generates ROCs
├── EMmej_sims_with_genomic_indel_len_dist_evaluating.py -> evaluates the performance 
├── generate_sims_with_genomic_indel_length_dist.py -> generate simulation files with indel
|           indel length distribution similar to that of a genomic data.
└── README.md
```